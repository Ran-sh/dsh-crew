{
  "id": "p0-isolate-dsh-crew-and-recover-official-harness",
  "mode": "IMPLEMENT",
  "source_branch": "fix/p0-isolated-dsh-profile",
  "source_commit": "LATEST",
  "objective": "Make dsh-crew fully isolated from the user's official DeepSeek Harness installation/profile/home, then recover the user's normal official Harness and uninstall all currently active local dsh-crew integration. After completion dsh-crew must remain UNINSTALLED locally until the user explicitly requests a final reinstall later.",
  "context": "P0 safety incident tracked by #32. Current main/install docs and scripts/setup.mjs hard-code the official persistent DSH web profile. Recent dsh-crew real-environment work used the user's normal DSH home/profile; afterward the user's official Harness stopped starting because the shared credential store became structurally incompatible with the installed credential loader. The user has explicitly required that dsh-crew-related installation/testing content must not modify official Harness state. Treat the official DSH home/profile/credentials as foreign user state, not project-owned state. This task has two phases in strict order: (A) fix repository install/test boundaries and validate deterministically; (B) recover the user's original official Harness, remove the legacy dsh-crew installation from the official web profile, uninstall Crew integrations/config locally, verify official Harness works independently, then leave Crew uninstalled. Do NOT reinstall Crew at the end. Use the user's currently installed DSH binary/version for recovery; do not upgrade/downgrade Harness as part of this task.",
  "allowed_changes": [
    "scripts/setup.mjs",
    "scripts/smoke-real.mjs",
    "scripts/live-crew-smoke.mjs",
    "scripts/live-policy-matrix.mjs",
    "src/install/install.mjs",
    "test/setup.test.mjs",
    "test/live-policy-matrix.test.mjs",
    "test/dsh-profile-isolation.test.mjs",
    "README.md",
    "README.*.md",
    "docs/agent-workflow.md",
    "docs/agent-results/p0-dsh-profile-isolation-and-uninstall-result.json",
    "local dsh-crew-owned state under ~/.config/dsh-crew/**",
    "local dsh-crew-owned DSH profiles/processes created by prior tests",
    "~/.codex/** only to remove dsh-crew-managed MCP/agent/prompt entries and preserve unrelated content/backups",
    "~/.claude/** only to remove dsh-crew-managed marketplace/plugin/permission/statusline entries and preserve unrelated content/backups",
    "the user's official DSH web profile only for one-time incident cleanup that removes @ran-sh/dsh-crew / dsh-crew bundle references introduced by this project, without changing any unrelated package/bundle/config",
    "the user's official DSH credential store only for incident recovery using a trustworthy pre-change backup or an exact installed-loader-proven reversible structural repair; credential values must never be changed, printed, uploaded, or committed"
  ],
  "forbidden_changes": [
    "src/install/install-legacy.mjs",
    ".github/workflows/**",
    "package.json",
    "pnpm-lock.yaml",
    "worker.cordis.yml",
    ".agent-workflow/**",
    "routing/model behavior unrelated to DSH installation isolation",
    "any official DSH profile/package/bundle/config unrelated to removing dsh-crew during incident cleanup",
    "provider/model secret values, API keys, tokens, cookies, private keys, signed URLs, credential plaintext, or raw credential files in GitHub/chat/result/log output",
    "copying credentials from the official DSH store into dsh-crew or test environments",
    "installing or starting dsh-crew in the user's official/default DSH_HOME after the repository fix",
    "reinstalling dsh-crew locally after cleanup",
    "force push, destructive reset/clean/stash of unrelated user work"
  ],
  "validation": [
    "Validate ACTIVE_TASK.json before any work and preserve unrelated dirty worktrees.",
    "Repository design: define a dsh-crew-owned DSH home separate from the official default DSH home. Use ~/.config/dsh-crew/harness (or the platform-equivalent derived from the supplied home) as the default Crew DSH_HOME; never default to ~/.dsh. Define dedicated profile name dsh-crew.",
    "Repository design: every DSH CLI invocation made by scripts/setup.mjs for install/uninstall/status must receive the isolated Crew DSH_HOME in its process environment and operate only on profile dsh-crew. Do not rely on the caller's ambient DSH_HOME. Export/test the constants/helpers needed to prove this.",
    "Repository design: a fresh dedicated dsh-crew profile must include the official in-box @deepseek-ai/dsh-web-app bundle plus @ran-sh/dsh-crew so it can expose its own web/Hub surface without modifying the official web profile. Use the DSH plugin/profile contract rather than editing profile package.json manually.",
    "Repository design: dedicate Hub port 3210 (or another single documented non-3080 Crew-owned port if 3210 is demonstrably unsuitable). Fresh dsh-crew config must point at the dedicated Hub URL, not http://127.0.0.1:3080. Legacy configs with the old project default 3080 must be safely treated/migrated as the former shared-profile default so future fresh/recovered installs do not silently reconnect to the official web profile. Preserve explicitly unrelated config fields.",
    "Repository design: scripts/smoke-real.mjs must not read ~/.dsh/.credentials.yaml, ~/.dsh/settings.yaml, OPENCODE_GO_API_KEY from files, or any official Harness store. Remove readDshStore-style behavior. Standalone smoke may use only explicitly supplied process environment credentials; if absent, exit as BLOCKED/SKIP-style non-success without inventing or reading credentials.",
    "Repository design: scripts/live-crew-smoke.mjs and scripts/live-policy-matrix.mjs must target an explicit DSH_CREW_HUB / dedicated 3210 default, never hard-code official port 3080. They must not start or mutate an official profile.",
    "Repository design: add a deterministic static/surface regression test (prefer test/dsh-profile-isolation.test.mjs) that fails if executable install/test paths reintroduce `--profile web`, `profiles/web`, direct reads of ~/.dsh credentials/settings, or a default Crew Hub target of 127.0.0.1:3080. Allow documentation to mention official web only as a read-only/non-target boundary, not as an install/test command.",
    "Repository design: update test/setup.test.mjs and related tests so fake profile roots are under the isolated Crew DSH_HOME/profiles/dsh-crew and dry-run logs explicitly say dedicated dsh-crew profile/home. Uninstall/status tests must prove the official ~/.dsh/profiles/web fixture is ignored.",
    "Documentation: update README.md, README.zh.md and all README.*.md install/update/uninstall sections so no normal command installs/removes dsh-crew in `--profile web`. Prefer the source setup installer as the safe cross-platform path because it controls DSH_HOME. Clearly state that the official/default Harness home/profile is not modified. Remove unsafe troubleshooting advice that tells users to edit ~/.dsh/profiles/web for Crew. If a translation cannot be safely rewritten, replace the unsafe install snippet with a pointer to the canonical safe install section instead of leaving the old command.",
    "Documentation: update docs/agent-workflow.md project adapter with a hard real-environment rule: all dsh-crew DSH tests use a disposable or Crew-owned isolated DSH_HOME and dedicated/disposable profile; never boot/install/mutate the user's normal ~/.dsh or persistent web profile; never read/copy official credential stores; credentials must be task-scoped explicit env or isolated-store credentials.",
    "Run node --test test/*.test.mjs and pnpm run build:client after repository changes. Do not perform any live dsh-crew test against the official/default DSH_HOME.",
    "Before local incident recovery, stop only dsh-crew disposable/test processes created by recent tasks. Identify the user's normal official Harness binary/profile and reproduce the current startup failure using the normal official command. Do not start Crew for this reproduction.",
    "Credential recovery: before mutation, create a restrictive local backup of the current official credential file and record only basename, mode, size, mtime and whole-file hash. Inspect the exact installed credential loader/schema and search for trustworthy pre-change/DSH-generated backups. Prefer restoring a trustworthy pre-change backup; otherwise only perform an exact loader-proven reversible structural repair that preserves every credential value. If neither is safe, stop BLOCKED rather than guessing. Never expose values.",
    "One-time legacy cleanup authorized by the user: using the user's existing installed DSH CLI/version and default official DSH_HOME, remove only @ran-sh/dsh-crew (and only its matching dsh-crew bundle reference) from the official persistent web profile if present. Do not remove/reorder/update any other dependency, bundle, patch, provider, model, setting or credential. Record secret-free before/after structural evidence showing only Crew references were removed.",
    "Remove local dsh-crew Codex and Claude integrations using the project uninstaller or equivalent narrowly scoped functions, preserving unrelated config and backups. Back up the Crew config if useful, then purge active ~/.config/dsh-crew runtime/config state so a later reinstall starts from the new isolated defaults; keep the repository checkout itself.",
    "Remove any dsh-crew-owned dedicated/disposable DSH profiles/homes/processes left by prior tests. Do NOT remove the user's official web/headless/other normal profiles.",
    "After cleanup, verify dsh-crew is not installed in the official web profile, not installed in Codex/Claude integrations, and no dsh-crew process/profile is active. Do not reinstall it.",
    "Start the user's original official DeepSeek Harness normally with the same installed binary and normal web profile. Verify healthy startup/service without dsh-crew present. This PASS is required for incident recovery. Leave the official Harness in its normal healthy state (running if that was the user's normal state) and leave Crew uninstalled.",
    "Write Result Contract v2 to docs/agent-results/p0-dsh-profile-isolation-and-uninstall-result.json. Include repository file list, deterministic test/build results, dedicated home/profile/port values, secret-free recovery method, official Harness startup result, uninstall status, and explicit `dsh_crew_reinstalled: false`. Stamp with validator --stamp, validate again, delete ACTIVE_TASK.json, and push only authorized repo files/result/ACTIVE deletion."
  ],
  "acceptance_criteria": [
    "No executable dsh-crew installer/test path defaults to the user's official DSH_HOME or `web` profile.",
    "scripts/setup.mjs owns an isolated Crew DSH_HOME and dsh-crew profile and never installs/uninstalls/status-checks Crew in the official web profile.",
    "Fresh Crew Hub defaults to its dedicated non-3080 endpoint and legacy shared-profile defaults cannot silently reconnect a recovered install to the official web profile.",
    "Repo real-smoke scripts do not read or copy official DSH credential/settings stores.",
    "README install/update/uninstall guidance no longer instructs users to install Crew into the official web profile, across shipped translations or via a safe canonical pointer.",
    "Agent Workflow explicitly forbids future real tests from touching the user's normal DSH_HOME/profile/credentials.",
    "All deterministic tests and client build PASS.",
    "The official DeepSeek Harness credential/profile state is safely recovered with no credential value changes or exposure, and the normal official Harness starts healthy without dsh-crew installed.",
    "Legacy dsh-crew references are removed from the official web profile without changing unrelated official profile content.",
    "Codex/Claude Crew integrations and active local Crew runtime/config are uninstalled/purged or safely backed up; the repository remains.",
    "At task completion dsh-crew is NOT reinstalled and no Crew profile/process is active. A later reinstall requires a new explicit user instruction.",
    "Result Contract v2 is stamped/valid and contains no secrets."
  ],
  "result_contract": "docs/agent-results/p0-dsh-profile-isolation-and-uninstall-result.json",
  "completion_commit_contract": [
    "scripts/setup.mjs",
    "scripts/smoke-real.mjs",
    "scripts/live-crew-smoke.mjs",
    "scripts/live-policy-matrix.mjs",
    "src/install/install.mjs",
    "test/setup.test.mjs",
    "test/live-policy-matrix.test.mjs",
    "test/dsh-profile-isolation.test.mjs",
    "README.md",
    "README.*.md",
    "docs/agent-workflow.md",
    "docs/agent-results/p0-dsh-profile-isolation-and-uninstall-result.json",
    "docs/agent-tasks/ACTIVE_TASK.json"
  ],
  "delete_active_task_on_completion": true,
  "metadata": {
    "tracks_issue": 32,
    "priority": "P0-safety-incident",
    "executor_scope": "repo-isolation-fix-plus-local-recovery-and-uninstall",
    "must_leave_crew_uninstalled": true,
    "future_reinstall_requires_user_instruction": true,
    "human_companion_generated": false
  }
}
