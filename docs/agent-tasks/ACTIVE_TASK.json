{
  "id": "TASK-V03-ISOLATED-RUNTIME-FINAL-ACCEPTANCE",
  "mode": "IMPLEMENT",
  "source_branch": "fix/v0.3-isolated-runtime-final-acceptance",
  "source_commit": "LATEST",
  "objective": "In one bundled pass, resolve the DSH CLI/runtime bootstrap blocker using Crew-owned isolated state, repair/reinstall the official DeepSeek Harness CLI/runtime if needed without mutating persistent official user state, then execute the complete final v0.3 real-environment acceptance and leave the machine in the correct final state.",
  "context": "The first final acceptance run was BLOCKED only because no runnable DSH CLI/runtime could be started. Deterministic tests/build passed and official Harness files remained byte/hash stable. The user now explicitly authorizes uninstalling/reinstalling the official DeepSeek Harness CLI/runtime if needed. Persistent official state under ~/.dsh (profiles/config/credentials/sessions) remains protected user data: preserve it, do not migrate credential values, and verify before/after integrity. Prefer a reusable Crew-owned DSH CLI/runtime path under Crew-owned isolated state rather than relying on a transient network npx fetch every invocation. After implementing the bootstrap, immediately perform the full final acceptance on the same branch. Batch the work; do not stop after only fixing bootstrap unless a genuine blocker prevents the live acceptance.",
  "allowed_changes": [
    "scripts/setup.mjs",
    "src/install/",
    "src/dsh-cli-runtime.mjs",
    "test/setup.test.mjs",
    "test/setup-isolation.test.mjs",
    "test/install.test.mjs",
    "test/dsh-cli-runtime.test.mjs",
    "test/dsh-profile-isolation.test.mjs",
    "docs/agent-results/v03-isolated-runtime-final-acceptance-result.json"
  ],
  "forbidden_changes": [
    "package.json, pnpm-lock.yaml, package-lock.json, yarn.lock, or other repository dependency/release metadata",
    ".github/workflows/**",
    "model routing, adaptive routing, workflow scheduling, retry/escalation, or business semantics unrelated to DSH CLI/bootstrap plumbing",
    "credential values, tokens, API keys, secrets, or secret-bearing payloads in repository files, logs, commits, or Result Contract",
    "destructive modification of persistent official ~/.dsh user state; official profile/config/credentials/sessions must be preserved across any runtime reinstall",
    "installing dsh-crew into the official web profile or using the official/default DSH_HOME for Crew",
    "force push, history rewrite, reset --hard, clean, or deletion of unrelated user files"
  ],
  "validation": [
    "node .agent-workflow/validator/validate-contract.mjs task docs/agent-tasks/ACTIVE_TASK.json",
    "implement deterministic Crew-owned DSH CLI resolution with precedence for explicit/Crew-owned reusable runtime before transient npx fallback; keep global dsh/npx as compatibility fallbacks",
    "prove status/uninstall probes do not trigger network downloads and Windows command/path quoting is deterministic",
    "run node --test test/*.test.mjs",
    "run pnpm run build:client",
    "capture non-secret before metadata/hash evidence for official ~/.dsh credential/config/profile files without exposing contents",
    "if official DeepSeek Harness CLI/runtime is missing or broken, uninstall/reinstall the official stable runtime as needed while preserving existing ~/.dsh persistent state; record exact runtime/package version used",
    "verify official Harness web profile starts normally after runtime repair/reinstall and before Crew final acceptance",
    "provision/use only Crew-owned isolated DSH state for Crew: DSH_HOME ~/.config/dsh-crew/harness, profile dsh-crew, Hub port 3210; no Crew install into official web profile",
    "install/status the integrated Crew from this branch using the reusable Crew-owned DSH runtime path",
    "run a genuine completed Hub/MCP worker and verify child_attempts[].selection_trace matches actual provider/model/source and contains no sensitive fields",
    "exercise live max_parallel raise/lower plus next-workflow and next-session activation boundaries",
    "enable adaptive routing only in the isolated Crew profile and verify warming/min-samples, automatic-candidate-only reorder, explicit priority preservation, restart clearing process-local health, and auditable adaptive trace",
    "exercise at least one bounded policy/provider/runtime failure and verify machine code propagation Hub -> client -> attempt -> workflow -> normalized failure without copying arbitrary vendor/secret payloads",
    "verify readiness/catalog diagnostics use observed evidence and keep info-only catalog hints non-failing",
    "run Standalone official execution only if explicitly authorized task-scoped or Crew-owned isolated credentials already exist; otherwise SKIP without reading official credential files",
    "run macOS smoke only on an actual macOS executor; otherwise SKIP",
    "exercise isolated Crew install/status/uninstall, verify no Crew residue affects official Harness, and compare official non-secret integrity evidence before/after",
    "if all applicable live gates PASS with only legitimate SKIPs, reinstall Crew only into the isolated Crew DSH_HOME/profile and leave it installed; if any live gate FAIL/BLOCKED, leave Crew uninstalled and official Harness healthy",
    "node .agent-workflow/validator/validate-contract.mjs result docs/agent-results/v03-isolated-runtime-final-acceptance-result.json --stamp"
  ],
  "acceptance_criteria": [
    "A reusable Crew-owned DSH CLI/runtime path exists and is preferred before transient npx download fallback; no global install is required for normal Crew operation.",
    "Installer/status/uninstall remain isolated to Crew-owned DSH_HOME/profile and do not mutate the official web profile.",
    "If official Harness runtime reinstall is performed, persistent ~/.dsh profile/config/credential state is preserved and official Harness is healthy afterward.",
    "All deterministic repository tests and client build pass after bootstrap changes.",
    "Official Harness non-secret integrity evidence is unchanged except for explicitly authorized runtime binaries/packages outside persistent user state.",
    "A genuine completed worker on the final integrated branch emits sanitized selection_trace matching actual execution.",
    "Live runtime controls, adaptive routing, structured error/failure diagnostics, and readiness/catalog diagnostics pass their real isolated Hub acceptance gates.",
    "Standalone is PASS only with explicitly authorized task-scoped/Crew-owned credentials; otherwise SKIP. macOS is PASS only on actual macOS; otherwise SKIP.",
    "Final local state is explicit: Crew installed only in isolated Crew state if all applicable live gates pass; otherwise Crew uninstalled. Official Harness must be healthy in either case.",
    "Result Contract schema_version 2 truthfully records PASS/FAIL/BLOCKED, includes second-precision timezone timeline, exact DSH runtime version/source, all legitimate SKIPs, and validator --stamp evidence.",
    "Completion commit contains only authorized implementation/test files, the Result Contract, and deletion of ACTIVE_TASK.json."
  ],
  "result_contract": "docs/agent-results/v03-isolated-runtime-final-acceptance-result.json",
  "completion_commit_contract": [
    "scripts/setup.mjs",
    "src/install/",
    "src/dsh-cli-runtime.mjs",
    "test/setup.test.mjs",
    "test/setup-isolation.test.mjs",
    "test/install.test.mjs",
    "test/dsh-cli-runtime.test.mjs",
    "test/dsh-profile-isolation.test.mjs",
    "docs/agent-results/v03-isolated-runtime-final-acceptance-result.json",
    "docs/agent-tasks/ACTIVE_TASK.json"
  ],
  "delete_active_task_on_completion": true,
  "metadata": {
    "executor": "ANY",
    "workflow_version": "1.8.0",
    "issues": "26,37",
    "bundled_task": true,
    "official_harness_runtime_reinstall_authorized": true,
    "protect_official_persistent_state": true,
    "isolated_dsh_home": "~/.config/dsh-crew/harness",
    "isolated_profile": "dsh-crew",
    "isolated_hub_port": 3210
  }
}
