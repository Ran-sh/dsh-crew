{
  "id": "TASK-V03-FINAL-OPENCODE-ACCEPTANCE",
  "mode": "TEST_ONLY",
  "source_branch": "test/v0.3-final-opencode-acceptance",
  "source_commit": "LATEST",
  "objective": "Complete the remaining v0.3 real-environment acceptance in one bundled pass using an executor-local OpenCode credential already configured for ZCode, without asking the user to create or provide a new key, while preserving official DeepSeek Harness persistent state and leaving dsh-crew installed only in Crew-owned isolated state if all applicable gates pass.",
  "context": "All normal v0.3 code work is merged. Main includes the reusable Crew-owned DSH runtime bootstrap and the schema-v3 live-policy-matrix I1 correction. The user explicitly authorizes this executor to use any already-configured OpenCode API credential/provider available inside the ZCode execution environment for acceptance. The credential is task-scoped test authorization only: never print, log, commit, upload, or copy the raw secret into repository files or the official/default DSH credential store. Do not ask the user for a new key. Prefer transient environment injection into the Crew-owned isolated runtime; if the executor must stage provider credentials inside Crew-owned isolated DSH state, keep them only there for the duration of acceptance and remove the executor credential before final cleanup/reinstall. The official/default ~/.dsh persistent profile/config/credentials remain protected user state. Crew acceptance uses only ~/.config/dsh-crew/harness, profile dsh-crew, Hub port 3210, and the reusable Crew-owned DSH runtime. If multiple already-configured OpenCode providers/credentials are available, the executor may choose any working one and may use more than one only to exercise adaptive candidate multiplicity; do not create new secrets. Standalone DeepSeek Official remains SKIP unless a separately authorized DeepSeek credential already exists. macOS remains SKIP unless the executor is actually on macOS.",
  "allowed_changes": [
    "docs/agent-results/v03-final-opencode-acceptance-result.json"
  ],
  "forbidden_changes": [
    "any tracked repository file outside docs/agent-results/v03-final-opencode-acceptance-result.json",
    "credential values, tokens, API keys, secrets, or raw secret-bearing payloads in repository files, logs, commits, or Result Contract",
    "reading, copying, migrating, or modifying credential values from the official/default ~/.dsh credential store",
    "installing dsh-crew into the official web profile or using the official/default DSH_HOME for Crew",
    "using Hub port 3080 for Crew acceptance work",
    "package.json, lockfiles, CI workflows, release metadata, source code, tests, or documentation",
    "routing/model/workflow semantic changes",
    "force push, history rewrite, reset --hard, clean, stash, or deletion of unrelated user files"
  ],
  "validation": [
    "node .agent-workflow/validator/validate-contract.mjs task docs/agent-tasks/ACTIVE_TASK.json",
    "node --test test/*.test.mjs",
    "pnpm run build:client",
    "capture non-secret before/after metadata/hash evidence for official ~/.dsh credential/config/profile files without exposing contents",
    "verify official Harness web profile is healthy before Crew install and again after acceptance cleanup/final state",
    "install dsh-crew only with Crew-owned DSH_HOME ~/.config/dsh-crew/harness, profile dsh-crew, Hub port 3210, using the reusable Crew-owned DSH runtime already supported by main",
    "use any executor-local OpenCode credential already configured for ZCode as task-scoped authorization; do not ask the user for a new key, do not print the key, and do not write it into official ~/.dsh",
    "make the chosen OpenCode credential available only to the Crew-owned isolated runtime, discover the usable provider/model through the isolated Harness/catalog, and record only non-secret provider/model/version metadata",
    "run node scripts/live-policy-matrix.mjs against the isolated Crew Hub and require all 15 matrix checks to pass, including schema-v3 review-pipeline I1",
    "run a genuine successful completed Hub/MCP worker through an OpenCode-backed provider/model and verify child_attempts[].selection_trace matches the actual provider/model/source and contains no sensitive fields",
    "exercise live max_parallel raise and lower admission semantics with genuine worker workloads plus next-workflow/next-session activation boundaries and activation metadata",
    "enable adaptive routing in isolated Crew state and verify warming/min_samples, automatic-candidate-only behavior, explicit priority preservation, process-restart health reset, and auditable adaptive selection_trace; use multiple already-configured OpenCode provider options if available and safe to exercise a real reorder, otherwise report exact catalog limitation without fabricating evidence",
    "exercise at least one bounded policy/provider/runtime failure and verify machine code propagation Hub -> client -> attempt -> workflow -> normalized failure without copying arbitrary vendor/secret payloads",
    "verify readiness/catalog diagnostics from the live isolated Hub use observed evidence and keep info-only catalog hints non-failing",
    "run Standalone DeepSeek Official only if a separately authorized task-scoped/Crew-owned DeepSeek credential already exists; otherwise SKIP without reading official credential files",
    "run macOS smoke only on an actual macOS executor; otherwise SKIP",
    "exercise isolated install/status/uninstall and confirm no residual Crew registration/process/config affects official Harness; compare official non-secret integrity evidence before/after",
    "remove any executor-local OpenCode secret material staged in Crew-owned isolated state before final state is established",
    "if every applicable live gate passes with only legitimate environment-specific SKIPs, reinstall dsh-crew only into Crew-owned isolated DSH_HOME/profile and leave it installed without persisting the executor's OpenCode secret; if any required live gate FAIL/BLOCKED, leave Crew uninstalled and official Harness healthy",
    "node .agent-workflow/validator/validate-contract.mjs result docs/agent-results/v03-final-opencode-acceptance-result.json --stamp"
  ],
  "acceptance_criteria": [
    "Result Contract schema_version is 2, has second-precision timezone timeline, and is validator-stamped truthfully.",
    "All deterministic repository tests and client build pass on the final main-derived branch.",
    "Official DeepSeek Harness remains healthy and its persistent ~/.dsh config/credential/profile integrity evidence is unchanged.",
    "Crew uses only ~/.config/dsh-crew/harness, profile dsh-crew, Hub port 3210, and the reusable Crew-owned DSH runtime.",
    "A ZCode-local already-configured OpenCode credential is sufficient for successful isolated worker execution; no new user-provided key is required and no raw secret appears in the repository or Result Contract.",
    "The live policy matrix is 15/15 PASS with the schema-v3 I1 expectation.",
    "A genuine successful worker emits sanitized selection_trace matching actual provider/model/source.",
    "Live runtime controls prove max_parallel raise/lower and documented activation boundaries on genuine workloads.",
    "Adaptive routing proves its applicable live invariants: warming, automatic-only adaptation, explicit priority authority, restart reset, and traceability; a real reorder is required when the available isolated catalog provides at least two eligible automatic candidates, otherwise the Result must state the exact non-secret catalog limitation instead of fabricating a reorder.",
    "Structured machine error codes and normalized failure classification survive a live bounded failure path without secret/vendor payload leakage.",
    "Readiness/catalog diagnostics reflect observed live evidence and do not treat info-only hints as failures.",
    "Standalone DeepSeek Official and macOS are SKIP only under their documented missing-credential/platform conditions.",
    "Install/status/uninstall is clean and does not affect official Harness persistent state.",
    "Any executor-local OpenCode credential material staged for testing is removed before completion. If all applicable gates pass, final local state has dsh-crew installed only in isolated Crew state without the executor secret persisted; otherwise Crew is uninstalled and official Harness is healthy.",
    "Only the Result Contract is added and ACTIVE_TASK.json is deleted in the completion commit."
  ],
  "result_contract": "docs/agent-results/v03-final-opencode-acceptance-result.json",
  "completion_commit_contract": [
    "docs/agent-results/v03-final-opencode-acceptance-result.json",
    "docs/agent-tasks/ACTIVE_TASK.json"
  ],
  "delete_active_task_on_completion": true,
  "metadata": {
    "executor": "ZCODE_PREFERRED",
    "workflow_version": "1.8.0",
    "main_baseline": "77682b124f150a4bc8e6494f66919c409b39ea14",
    "issue": 26,
    "final_acceptance": true,
    "credential_authorization": "executor-local preconfigured OpenCode credential",
    "isolated_dsh_home": "~/.config/dsh-crew/harness",
    "isolated_profile": "dsh-crew",
    "isolated_hub_port": 3210
  }
}
