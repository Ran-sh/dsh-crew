{
  "id": "TASK-V033-RELEASE-GATE-FINAL",
  "mode": "TEST_ONLY",
  "source_branch": "main",
  "source_commit": "LATEST",
  "objective": "Run the final non-publishing release gate for DSH Crew v0.3.3 and return RELEASE_READY only if the exact product candidate is safe to publish without source changes.",
  "context": "Issue #49 tracks the final v0.3.3 release gate. The implementation completion commit is 369b141f7878e057d08c673fe90b62185e67fdeb and TASK-V033-NPX-LIFECYCLE is PASS. Treat 369b141f7878e057d08c673fe90b62185e67fdeb as the candidate product release target unless this gate finds evidence that requires BLOCKED. This task must not publish npm, mutate dist-tags, create/move/delete tags, create a GitHub Release, modify credentials, weaken package-manager policies, or change product/source files. Preserve the real machine's installed DSH Crew state and use disposable homes/profiles for lifecycle tests.",
  "allowed_changes": [
    "docs/agent-results/TASK-V033-RELEASE-GATE-FINAL-result.json"
  ],
  "forbidden_changes": [
    "package.json or any product/source/test/docs implementation file",
    "npm publish or dist-tag mutation",
    "git tag creation, movement, rewrite, or deletion",
    "GitHub Release creation or mutation",
    "credentials, tokens, OTPs, auth headers, .npmrc contents, or credential stores",
    "normal ~/.dsh, official DSH web profile, or official DSH credentials",
    "user pnpm/npm security policy weakening or lifecycle-script approvals",
    "unrelated branches, worktrees, or machine state"
  ],
  "validation": [
    "Validate docs/agent-results/TASK-V033-NPX-LIFECYCLE-result.json and confirm status PASS with no blockers.",
    "Confirm package.json, src/runtime-identity.mjs, and CHANGELOG.md identify version 0.3.3 and package.json exposes exactly one bin dsh-crew -> ./bin/dsh-crew.mjs.",
    "pnpm install --frozen-lockfile",
    "Run focused npx lifecycle / installer / distribution tests and node --test test/*.test.mjs.",
    "pnpm run build:client and restore any generated-only drift so the source checkout remains clean.",
    "pnpm run verify:npm-install",
    "pnpm run verify:npm-install:official",
    "npm pack --dry-run --json and audit package identity, required payload, single bin, file count, and forbidden entries.",
    "npm publish --dry-run --access public only; do not perform a real publish.",
    "Using a locally packed 0.3.3 candidate in a disposable HOME, exercise real npm exec/npx semantics for install -> status -> update -> status -> uninstall -> status. Delete transient tarball/npx cache inputs after install and prove the persisted Crew-owned payload remains runnable; verify same-version update idempotence and repair behavior without changing config/.env bytes.",
    "Re-check source-checkout node scripts/setup.mjs install -> status -> uninstall compatibility in a separate disposable home.",
    "Read-only registry/tag/Release checks: @ran-sh/dsh-crew@0.3.3 must be absent, scoped latest must be 0.3.2, v0.3.3 tag absent, GitHub Release v0.3.3 absent, and existing v0.3.0/v0.3.1/v0.3.2 tag targets unchanged.",
    "Confirm final real machine state remains INSTALLED and no task-created disposable worktrees/processes are left behind."
  ],
  "acceptance_criteria": [
    "Result status PASS with summary containing release_gate: RELEASE_READY.",
    "Result records immutable_release_target=369b141f7878e057d08c673fe90b62185e67fdeb, or BLOCKED if this exact commit cannot safely be released.",
    "Full deterministic suite, targeted lifecycle tests, build, both npm install verifiers, pack audit, and publish dry-run all pass.",
    "Real disposable npx lifecycle proves durable install, read-only status, idempotent/repair-capable update, config-preserving uninstall, and no dependence on transient npx cache/tarball paths.",
    "Public state remains unchanged: npm 0.3.3 absent/latest 0.3.2, no v0.3.3 tag, no v0.3.3 GitHub Release.",
    "No credentials or official DSH state are read or modified; final_machine_state: INSTALLED.",
    "Completion commit changes only the Result Contract and deletes ACTIVE_TASK.json."
  ],
  "result_contract": "docs/agent-results/TASK-V033-RELEASE-GATE-FINAL-result.json",
  "completion_commit_contract": [
    "docs/agent-results/TASK-V033-RELEASE-GATE-FINAL-result.json",
    "docs/agent-tasks/ACTIVE_TASK.json"
  ],
  "delete_active_task_on_completion": true,
  "metadata": {
    "executor": "ANY",
    "issue": 49,
    "candidate_release_target": "369b141f7878e057d08c673fe90b62185e67fdeb"
  }
}