DSH CREW v0.2 RUNTIME HARDENING — REAL ENVIRONMENT VALIDATION
================================================================

ROLE
----
You are a TESTER ONLY. Do not implement fixes. Do not edit source code. Do not commit, push, merge, rebase, reset, clean, stash, or change the PR. Do not change credentials. Do not print API keys/tokens/secrets.

Repository: Ran-sh/dsh-crew
Code branch: fix/v0.2-runtime-hardening
Code commit under test: 15859546ef31f5dc49fc17075a7f80f7d509b54a
Draft PR: #1

The current branch may contain this temporary test instruction commit on top of the code commit. That is expected. The runtime code under test is commit 15859546ef31f5dc49fc17075a7f80f7d509b54a.

IMPORTANT MODEL / PROVIDER CONSTRAINTS
--------------------------------------
1. If the test orchestrator is running through the OpenCode Go plan, use Muse as the orchestrator/tester model only.
2. DSH worker model validation must use the OpenCode Go API/provider already configured in DeepSeek Harness.
3. For worker-model coverage, use model IDs actually advertised by the current DSH catalog that belong to the MiMo and Qwen families. Do not invent model IDs. Record the exact provider + model IDs observed.
4. MiMo/Qwen are WORKER models for these tests; do not silently substitute them as the main tester model.
5. If the environment is not already configured for the required OpenCode Go provider / MiMo / Qwen models, mark the affected case BLOCKED. Do not rewrite credentials or global provider configuration just to make the test pass.
6. Standalone mode is a separate compatibility path. It currently depends on the DeepSeek Official standalone path / DEEPSEEK_API_KEY. Test it only if that credential is already available and authorized. Otherwise mark Standalone SKIP; do not fabricate or request a key.

GLOBAL RULES
------------
- Testing only. No source edits.
- Prefer disposable temporary Git repositories for coding tests. Never ask a worker to edit the dsh-crew source checkout.
- Session-level changes through dsh_worker_config are allowed and should be reset after each group.
- Do not modify ~/.config/dsh-crew/config.json, DSH provider credentials, or model-priority files during this run.
- If a case requires unavailable global configuration, report BLOCKED instead of changing it.
- Keep exact evidence: workflow_id, child attempt id, phase, provider, model, selection_source, timed_out, candidate/review flags.
- Do not treat a model's prose claim as proof. Prefer returned workflow metadata, DSH Hub session state, Git diff, and test command output.
- At the end, restore session config with dsh_worker_config(reset=true).

PRE-FLIGHT
----------
P0. Verify repository / commit.
- Confirm the fix branch contains commit 15859546ef31f5dc49fc17075a7f80f7d509b54a.
- Record current OS, Node version, pnpm version, DSH version, host/orchestrator, and whether DSH Hub is reachable.
- Do not fail merely because the temporary prompt commit is newer than the code commit.

P1. Read dsh_worker_config with no arguments.
Record:
- enabled
- hub_reachable
- effective/default tier or role policy
- collaboration mode
- escalation setting
- effective worker model selections for flash/pro if reported
- provider resolution errors if any
Do not expose secrets.

P2. Confirm model catalog / provider evidence.
- Identify the exact OpenCode Go provider id currently visible to DSH.
- Identify at least one MiMo-family model id and one Qwen-family model id that are actually available.
- Record whether current flash/worker and pro/strong/reviewer slots resolve to those models.
- If required model families are visible but not selected by the current policy, report this fact. Do not alter global priorities.

DISPOSABLE TEST REPO
--------------------
Create a temporary Git repository outside the dsh-crew checkout. It should contain a tiny Node project with a deterministic test, for example:
- value.mjs exports VALUE = 1
- value.test.mjs expects VALUE === 2
Commit the initial baseline.
All coding-worker tests below must use this disposable repository (or a fresh copy) as cwd.

TEST MATRIX
===========

T1 — HUB BLOCKING WORKER / DEFAULT MODEL
----------------------------------------
1. dsh_worker_config(reset=true, mode="hub", collaboration_mode="balanced", escalate_on_failure=false, pro_reviews_flash=false)
2. Call dsh_run_worker with role="worker", cwd=<fresh disposable repo>, and a self-contained task to change VALUE from 1 to 2 and run the test.
3. Require final workflow status done/completed.
4. Verify returned child attempt metadata contains a real Hub attempt id and exact provider/model/selection_source.
5. Verify candidate is available and corresponds to the actual Git change.
6. Verify worker delivery report contains Diff / Tests / Risks and test evidence is PASS.
7. Verify the primary dsh-crew checkout was not modified.
PASS requires both workflow metadata and real Git/test evidence.

T2 — LEGACY MODEL-CLASS HINT IS PRESERVED
------------------------------------------
Use fresh disposable repos for each call.
A. Call dsh_run_worker(role="worker", tier="flash", ...)
B. Call dsh_run_worker(role="worker", tier="pro", ...)
Record provider/model/selection_source for both.
Expected:
- both remain role=worker;
- tier acts only as model-class hint;
- pro must not accidentally collapse back to the flash/cheap slot when a distinct strong slot is configured.
If current global model policy intentionally maps both slots to the same exact model, mark this case PASS-WITH-NOTE rather than inventing a difference; evidence must show the resolver followed policy.

T3 — MIMO / QWEN WORKER COVERAGE
--------------------------------
Use the model slots already configured by DSH Crew / DSH. Do not rewrite priorities.
- Run at least one successful worker workflow whose returned metadata proves a MiMo-family model was used, if currently selectable.
- Run at least one successful worker or reviewer workflow whose returned metadata proves a Qwen-family model was used, if currently selectable.
- For each, use a fresh disposable repo and verify actual file/test result.
If one family is visible in the catalog but cannot be selected under the existing policy, mark BLOCKED and state exactly why.

T4 — REVIEW PIPELINE / REVIEWER NON-MUTATION
--------------------------------------------
1. dsh_worker_config(reset=true, mode="hub", collaboration_mode="review-pipeline", escalate_on_failure=false, pro_reviews_flash=true)
2. Run a simple successful coding worker in a fresh disposable repo.
3. Expect worker + reviewer child attempts in the same workflow.
4. Record exact provider/model for worker and reviewer.
5. Review must have a verdict and review metadata.
6. Verify reviewer did not mutate the candidate. The final candidate fingerprint / Git state must remain the implementation candidate.
7. If reviewer mutation is detected, an "approved" verdict must NOT remain green; expected normalized verdict is request_changes / invalidated.

T5 — ASYNC SPAWN / STATUS / RESULT PARITY
-----------------------------------------
1. Reset session to Hub, balanced, no auto review, no escalation.
2. dsh_spawn_worker on a fresh disposable repo.
3. Immediately record returned wf-* workflow_id.
4. Call dsh_worker_status while running or queued.
5. Call dsh_worker_result(job_id=<wf-id>, wait_seconds=...) until terminal.
6. Verify final semantic result matches the blocking workflow contract: phase/status, candidate, child attempt metadata, provider/model, delivery evidence.
7. Ensure dsh_worker_result accepts wf-* ids as documented.

T6 — REAL CANCELLATION TARGETS THE ACTIVE HUB ATTEMPT
-----------------------------------------------------
Goal: prove workflow cancellation reaches the actual hub-* attempt, not a synthetic wf-* attempt id.
1. Reset session: mode="hub", collaboration_mode="balanced", escalate_on_failure=false, pro_reviews_flash=false.
2. Spawn a worker task in a fresh disposable repo that deliberately runs a long harmless command (for example a 60-second Node sleep) before completion.
3. As soon as dsh_worker_status shows it running, call dsh_worker_cancel(job_id=<wf-id>).
4. Fetch dsh_worker_result.
Expected:
- workflow terminal status cancelled;
- no escalation/reviewer attempt starts after cancellation;
- underlying Hub session/attempt is cancelled/stopped;
- child attempt evidence contains the real hub-* attempt id when available.
FAIL if the wf-* wrapper says cancelled but the underlying Hub attempt keeps executing.

T7 — ATTEMPT TIMEOUT CANCELS BEFORE ANY RETRY
---------------------------------------------
Goal: prove timeout does not leave two live agents overlapping.
1. dsh_worker_config(reset=true, mode="hub", collaboration_mode="balanced", default_timeout_seconds=8, escalate_on_failure=false, pro_reviews_flash=false)
2. Run/spawn a harmless task that executes a command sleeping significantly longer than 8 seconds.
3. Observe workflow/result metadata.
Expected:
- active attempt is cancelled/stopped at timeout;
- child attempt is marked timed_out=true when exposed;
- workflow reaches a terminal failure/blocked state according to policy;
- there is no second overlapping worker because escalation is disabled.
Use DSH Hub session state as evidence that the timed-out attempt is no longer running.

T8 — ESCALATION POLICY (NON-DESTRUCTIVE)
----------------------------------------
1. dsh_worker_config(reset=true, mode="hub", collaboration_mode="balanced", escalate_on_failure=true, pro_reviews_flash=false)
2. Read config report and confirm escalation is effective.
3. Do NOT intentionally corrupt global model/provider config.
4. If a natural, safe worker failure/incomplete delivery occurs during testing, verify the next attempt uses the stronger model policy and that candidate capture is refreshed after the latest attempt.
5. If no natural escalation occurs, report NOT EXERCISED rather than forcing an unreliable/expensive failure.
Important stale-candidate assertion: if the latest candidate capture fails, final candidate_available must be false; an earlier attempt's candidate must not be presented as final, and the worktree must be retained for recovery.

T9 — STANDALONE COMPATIBILITY (OPTIONAL)
----------------------------------------
Only run if DEEPSEEK_API_KEY / DeepSeek Official standalone access is already configured and authorized.
1. dsh_worker_config(reset=true, mode="standalone", collaboration_mode="balanced", escalate_on_failure=false)
2. Run one tiny worker task in a disposable repo.
3. Verify job completes and reports standalone job/attempt metadata.
4. Verify coding result + Git/test evidence.
5. Do not expect OpenCode Go / MiMo / Qwen on this path; note that Standalone is a DeepSeek Official compatibility path.
If credential is absent: SKIP, not FAIL.

T10 — SESSION RESET / SAFETY
----------------------------
1. Call dsh_worker_config(reset=true).
2. Verify session settings return to defaults.
3. Verify dsh-crew source checkout is unchanged except for the already-existing temporary test prompt commit in Git history.
4. Verify no API keys/tokens were printed into logs or the report.
5. Delete disposable temp repos only if they are definitely inside the temp directory you created; never clean the dsh-crew checkout.

PASS / FAIL POLICY
==================
Use only these statuses:
PASS
PASS-WITH-NOTE
FAIL
BLOCKED
SKIP
NOT EXERCISED

A case is FAIL only when the product behavior contradicts the expected runtime contract.
A missing provider/model/credential that the tester is forbidden to configure is BLOCKED or SKIP, not FAIL.

FINAL REPORT FORMAT
===================
Return ONE report in your chat/terminal output. Do not write or commit a result file unless the user explicitly asks.

# DSH Crew Runtime Hardening — Real Environment Test Report

## Environment
- code commit:
- OS:
- Node:
- pnpm:
- DSH:
- orchestrator/tester model:
- Hub reachable:
- OpenCode Go provider id:
- MiMo model id(s):
- Qwen model id(s):

## Results
| Case | Status | Provider / Model | Workflow / Attempt IDs | Evidence | Notes |
|---|---|---|---|---|---|
| T1 Hub blocking | | | | | |
| T2 tier hint | | | | | |
| T3 MiMo/Qwen | | | | | |
| T4 review pipeline | | | | | |
| T5 async parity | | | | | |
| T6 cancellation | | | | | |
| T7 timeout | | | | | |
| T8 escalation | | | | | |
| T9 standalone | | | | | |
| T10 reset/safety | | | | | |

## Failures
For every FAIL include:
- exact reproduction steps
- expected behavior
- actual behavior
- workflow_id
- child attempt id(s)
- provider/model/selection_source
- relevant phase/status/error_code/timed_out flags
- concise log excerpt with secrets removed

## Verdict
Choose exactly one:
- READY FOR MERGE
- NOT READY — FIX REQUIRED
- INCOMPLETE — BLOCKED BY ENVIRONMENT

STOP RULE
=========
After producing the report, STOP. Do not fix anything. Do not commit anything. Do not push anything. Do not merge PR #1. Return control to the user.